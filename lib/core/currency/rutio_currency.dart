abstract final class RutioCurrency {
  /// Brand symbol used when we need a compact inline currency mark in text.
  static const String coinSymbol = 'R';

  /// Legacy alias kept for compatibility with older callers.
  static const String coinEmoji = coinSymbol;
}
